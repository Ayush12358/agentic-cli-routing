# Release v0.1.0

Assets:

- `release/agentic-cli-routing-v0.1.0.tar.gz` (stored in repository)

Download & install:

```bash
bash download-install.sh Ayush12358/agentic-cli-routing v0.1.0
```

Direct raw download URL:

https://raw.githubusercontent.com/Ayush12358/agentic-cli-routing/main/release/agentic-cli-routing-v0.1.0.tar.gz

This release stores the tarball in the repository under `release/`. Use `download-install.sh` to download and install it into `.github/skills/agentic-cli-routing` and perform cleanup.
